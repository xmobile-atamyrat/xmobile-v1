#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# All suites under tests/: Vitest (unit, client, integration) + Playwright (e2e).
# Requires Docker (Testcontainers) and Playwright browsers (`yarn test:e2e:install` once).
# Skips with: git push --no-verify
yarn test:all
